# 디자인.md — 시험 대비 사이트 모양 규칙

이 파일은 시험 대비 사이트(`시험기간 화이팅!`)의 **모양을 정하는 유일한 기준**입니다.
화면을 새로 만들거나 고칠 때는 여기 적힌 값만 씁니다. 여기 없는 스타일은 만들지 않습니다.
바꾸고 싶은 것이 생기면 **이 파일을 먼저 고치고, 그다음에 코드를 고칩니다.**

여기 적힌 값은 새로 지어낸 것이 아니라 지금 사이트가 이미 쓰고 있는 값입니다.
그래서 이 규칙을 지키면 화면이 지금과 달라지지 않습니다.

> 마지막 확인: 2026-09-12 · 대상: `index.html`, `app/css/style.css`

---

## 0. 지키는 방법

- 색·모서리·글꼴은 전부 `app/css/style.css` 맨 위의 변수(`:root`)에서 가져옵니다.
- 화면 코드나 인라인 `style=` 안에 색깔 값(`#2f6df6` 같은 것)을 직접 적지 않습니다.
  직접 적으면 어두운 모드에서 반드시 깨집니다.
- 새 클래스를 만들기 전에 이미 있는 `.card` `.btn` `.choice` `.explain` `.row` 로 되는지 먼저 봅니다.

---

## 1. 바탕색과 글자색

### 쓰는 색 (이 목록이 전부입니다)

| 자리 | 변수 | 밝은 모드 | 어두운 모드 |
|---|---|---|---|
| 화면 바탕 | `--bg` | `#f6f7f9` | `#14161a` |
| 카드 바탕 | `--surface` | `#ffffff` | `#1c1f25` |
| 카드 안쪽 옅은 바탕 (해설 등) | `--surface-2` | `#f0f2f5` | `#23272e` |
| 테두리 | `--line` | `#e2e5ea` | `#2f343c` |
| 본문 글자 | `--text` | `#1a1d23` | `#e8eaee` |
| 보조 글자 | `--text-mid` | `#5b6472` | `#a3abb8` |
| 흐린 글자 | `--text-dim` | `#8b94a3` | `#767f8d` |
| 강조 (링크·주 버튼) | `--accent` | `#2f6df6` | `#5b8dff` |
| 맞음 | `--ok` / `--ok-soft` | `#17864f` / `#e2f5eb` | `#4ec98a` / `#17352a` |
| 틀림 | `--bad` / `--bad-soft` | `#cf3b41` / `#fdebec` | `#f0777c` / `#3a1f22` |
| 주의 (출처 없음) | `--warn` / `--warn-soft` | `#b7791f` / `#fdf3e0` | `#e0a94a` / `#35291b` |

### 대비 규칙 (휴대폰에서 읽히게 하는 부분)

- **읽어야 하는 글자는 바탕과의 대비가 4.5:1 이상입니다.** 작은 글자라고 봐주지 않습니다.
- 문제 지문·선택지·해설·출처는 `--text` 또는 `--text-mid` 만 씁니다.
  - `--text` 는 흰 바탕에서 약 16:1, `--text-mid` 는 약 6:1 이라 둘 다 통과합니다.
- **`--text-dim` 은 흰 바탕에서 3.1:1 밖에 안 됩니다. 읽어야 하는 글자에 쓰지 않습니다.**
  못 읽어도 문제 푸는 데 지장이 없는 곳(진행 표시 `3/10`, 통계 라벨, 빈 목록 안내)에만 씁니다.
- 색이 있는 글자(`--ok` 4.6:1, `--bad` 4.8:1, `--accent` 4.5:1)는 흰 바탕에서 턱걸이로 통과합니다.
  그래서 **이 색으로 쓰는 글자는 굵기 600 이상**으로 둡니다. 얇게 쓰면 안 읽힙니다.
- 어두운 모드에서 순수 검정(`#000`)과 순수 흰색(`#fff`) 글자는 쓰지 않습니다. 눈이 아픕니다.

### 눈으로 판단하는 법

휴대폰 밝기를 30% 까지 낮추고 창가에서 화면을 봅니다.
**해설 글자와 출처 한 줄이 그대로 읽히면 통과, 눈을 찡그리게 되면 실패입니다.**

---

## 2. 글자 크기

| 자리 | 크기 | 굵기 | 줄 간격 |
|---|---|---|---|
| 화면 제목 (`.page-head h1`) | 21px (폰 19px) | 600 | 1.3 |
| 구역 제목 (`.section-head h2`) | 15px | 600 | 1.4 |
| **문제 지문** (`.q-prompt`) | **16.5px** | 400 | **1.65** |
| 선택지 (`.choice`) | 14px | 400 | 1.55 |
| OX 선택지 | 22px | 700 | — |
| 플래시카드 앞뒤 | 17px | 400 | 1.7 |
| 채점 결과 한 줄 (`.verdict`) | 14px | 600 | — |
| **해설** (`.explain`) | **13.5px** | 400 | **1.7** |
| 해설 끝 출처 한 줄 | 12.5px | 400 | 1.6 |
| 본문·목록·버튼 글자 | 13.5px | 400 | 1.6 |
| 보조 설명 | 12.5px | 400 | 1.6 |
| 숫자 배지 | 11px | 600 | — |

규칙:

- **화면에서 제일 먼저 읽어야 하는 것은 문제 지문입니다. 그래서 본문보다 크게(16.5px) 둡니다.**
  해설을 지문보다 크게 만들지 않습니다.
- **12.5px 아래로 내려가는 글자는 만들지 않습니다.** 11px 은 동그란 숫자 배지에만 씁니다.
- 한글 문단에는 전부 `word-break: keep-all` 을 겁니다. 단어가 줄 끝에서 잘리면 읽는 속도가 떨어집니다.
- 크기를 0.5px 단위로 새로 만들지 않습니다. 위 표에 있는 값 중에서 고릅니다.
- 소개 페이지(`index.html`)의 큰 제목 34px(폰 27px)만 예외입니다. 앱 안에서는 21px 이 최대입니다.

---

## 3. 문제 한 개가 차지하는 영역

- **문제 한 개 = 카드 한 개이고, 한 화면에는 문제가 하나만 나옵니다.** 스크롤로 여러 문제를 늘어놓지 않습니다.
- 카드 모양: 바탕 `--surface`, 테두리 1px `--line`, 모서리 12px, 안쪽 여백 16px(넓게 쓸 때 22px), 그림자는 아주 옅은 한 겹만.
- 카드 폭은 본문 최대 폭 1000px 안에서 가운데로 놓습니다. 문제 지문을 이보다 넓게 늘리지 않습니다. 한 줄이 길면 눈이 다음 줄 첫 글자를 못 찾습니다.
- 폰(가로 760px 이하)에서는 좌우 여백 14px, 카드가 화면 폭을 꽉 채웁니다.

### 카드 안의 순서 (화면마다 바꾸지 않습니다)

1. 위 줄 — 왼쪽에 과목·단원·유형·난이도 뱃지, 오른쪽에 진행 표시(`3 / 10`)
2. 문제 지문
3. 선택지
4. 채점 결과 한 줄
5. 해설
6. 출처 한 줄
7. 맨 아래 버튼 줄

### 선택지

- **선택지는 항상 세로로 쌓습니다.** 가로 2열로 늘어놓지 않습니다. OX 두 칸만 예외로 가로입니다.
- 선택지 한 칸은 왼쪽에 번호 상자(20px 정사각), 오른쪽에 글입니다. 글은 왼쪽 정렬입니다.
- 선택지 한 칸의 누르는 높이는 **최소 44px** 입니다. 손가락으로 눌러야 합니다.
- 칸 사이 간격 8px, 모서리 8px, 테두리 1px.
- **문제를 풀기 전에는 정답도 해설도 화면 어디에도 보이지 않습니다.** 접어 두는 것이 아니라 아예 그리지 않습니다.

---

## 4. 정답일 때와 틀렸을 때의 표시

**색만으로 알리지 않습니다. 색 + 글자 + 자리, 세 가지를 같이 씁니다.**
색만 쓰면 색약인 사람과 밝은 햇빛 아래에서 구분이 안 됩니다.

### 맞았을 때

- 고른 선택지: 테두리 `--ok`, 바탕 `--ok-soft`, 번호 상자는 `--ok` 로 채우고 글자는 흰색.
- 선택지 바로 아래 결과 줄: 바탕 `--ok-soft`, 글자 `--ok`, 굵기 600, **"정답입니다"** 라고 글자로 적습니다.

### 틀렸을 때

- 고른 선택지: 테두리 `--bad`, 바탕 `--bad-soft`, 번호 상자는 `--bad` 로 채우고 글자는 흰색.
- **정답인 선택지는 같이 초록으로 표시합니다.** 무엇이 답이었는지 반드시 보여 줍니다.
- 결과 줄: 바탕 `--bad-soft`, 글자 `--bad`, 굵기 600, **"틀렸습니다 — 정답은 ③"** 처럼 정답 번호까지 글자로 적습니다.

### 공통

- **결과 줄은 언제나 선택지 바로 아래 같은 자리에 나옵니다.** 팝업이나 잠깐 떴다 사라지는 알림으로 띄우지 않습니다. 사라지면 다시 볼 수 없습니다.
- **해설은 채점하는 순간 펼쳐진 채로 나옵니다.** 따로 눌러야 보이게 하지 않습니다.
- 해설 마지막 줄에 출처를 붙입니다. 근거를 못 찾은 문제는 `--warn` 색과 `--warn-soft` 바탕으로 **"자료에 없음"** 이라고 눈에 띄게 적습니다.
- 채점이 끝난 선택지는 더 눌리지 않게 잠급니다.
- 서술형은 자동 채점하지 않습니다. 모범답안과 키워드를 펼쳐 두고 본인이 맞음/틀림을 고릅니다. 고른 뒤의 결과 줄 모양은 위와 똑같습니다.
- **초록은 맞음, 빨강은 틀림.** 이 두 색을 다른 뜻으로 쓰지 않습니다.
  (진도 막대의 초록·빨강은 "다 함 / 밀림" 이라는 다른 뜻이므로, 문제 카드 안에서는 진도 막대를 쓰지 않습니다.)

---

## 5. 버튼 모양과 위치

### 모양

- 모서리 8px, 테두리 1px, 안쪽 여백 `8px 14px`(폰에서는 `9px 14px`), 글자 13.5px.
- **누르는 자리는 최소 44 × 44px** 입니다.
- 종류는 네 가지뿐이고, 새로 만들지 않습니다.

| 종류 | 모양 | 쓰는 곳 |
|---|---|---|
| 주 버튼 `.btn-primary` | `--accent` 로 채우고 흰 글자, 굵기 600 | 그 화면에서 하려는 단 하나의 일 (채점하기, 다음 문제) |
| 보통 버튼 `.btn` | 흰 바탕, 회색 테두리, 검은 글자 | 나머지 전부 |
| 위험 버튼 `.btn-danger` | 흰 바탕, `--bad` 글자 | 지우기·초기화 |
| 작은 버튼 `.btn-sm` | 위와 같고 글자 12.5px | 목록 줄 오른쪽 끝 |

- **한 화면에 파랑으로 채운 주 버튼은 하나만 둡니다.** 둘이면 무엇을 눌러야 할지 모르게 됩니다.
- 알약 모양, 동그란 버튼, 테두리 없는 글자 버튼을 새로 만들지 않습니다.
- 아이콘만 있는 버튼을 만들지 않습니다. 반드시 글자가 있습니다.

### 위치

- **문제 카드의 버튼은 카드 맨 아래 한 줄에 모읍니다.** 지문 옆이나 선택지 사이에 끼워 넣지 않습니다.
- 그 줄에서 **주 버튼은 오른쪽 끝**, 보조 버튼(건너뛰기, 오답노트에 담기)은 그 왼쪽에 둡니다.
  다음 문제로 넘어가는 버튼은 문제가 바뀌어도 **언제나 같은 자리**에 있어야 합니다. 손가락이 자리를 외웁니다.
- 폰에서는 주 버튼을 가로 전체로 늘립니다(`.btn-block`).
- **화면 아래에 떠 있는 고정 버튼은 쓰지 않습니다.** 해설 마지막 줄을 가립니다.

### 글자

버튼 글자에는 무슨 일이 일어나는지 적습니다.
"확인" 대신 **"채점하기"**, "저장" 대신 **"오답노트에 담기"**, "예" 대신 **"기록 전부 지우기"**.

---

## 6. 쓰지 말 것

### 애니메이션

- **움직이는 것은 전부 쓰지 않습니다.** 상태가 바뀌면 그 순간 바뀐 모양으로 있습니다.
- 구체적으로 금지: 나타나기·사라지기 효과, 카드 뒤집기, 눌렀을 때 튀어오르거나 떠오르는 버튼(`transform: translateY`), 진도 막대가 스르륵 차오르는 연출(`transition: width`), 점수가 올라가는 숫자 세기, 화면 전환 효과, 반짝이는 강조.
- **딱 하나 허용**: 마우스를 올렸을 때 배경색이 바뀌는 정도의 0.12초 색 전환.
  위치·크기가 변하는 전환은 허용하지 않습니다.
- 이유: 시험 기간에 같은 화면을 수백 번 봅니다. 한 번 예쁜 움직임은 백 번째에 방해가 됩니다.
  그리고 채점 결과가 스르륵 나타나면 정답을 확인하는 0.3초가 매번 낭비됩니다.

### 그림자 남발

- **그림자는 카드 한 겹(`--shadow`)만** 씁니다.
- 버튼, 선택지, 입력칸, 뱃지에는 그림자를 넣지 않습니다.
- 그림자를 겹치거나 진하게 해서 깊이를 만들지 않습니다. 깊이는 테두리 1px 과 바탕색 차이로 냅니다.
- 색이 있는 그림자(파란 버튼 아래 파란 빛 같은 것)를 쓰지 않습니다.

### 읽기 어려운 얇은 글씨

- **`font-weight: 300` 이하를 쓰지 않습니다.** 본문 400, 강조 600, 제목 600~700 입니다.
- 흐린 회색 글자를 얇게까지 만들지 않습니다. 흐리면 흐린 대로, 굵기는 400 을 지킵니다.
- `letter-spacing` 을 음수로 넓게 걸지 않습니다. 제목에 `-0.02em` 까지만 씁니다.

### 그 밖에 쓰지 않는 것

- 바탕에 깔리는 사진·무늬·그라데이션 (글자와 겹쳐서 대비를 무너뜨립니다)
- 그라데이션으로 칠한 글자
- 한글 이탤릭
- 몇 초 뒤 저절로 사라지는 안내로 중요한 내용을 알리는 것
- 밑줄 없는 링크 (본문 안 링크는 색과 밑줄을 같이 씁니다)

---

## 7. 어두운 모드

- 밝은 모드에서 정한 자리마다 어두운 모드 값이 하나씩 짝으로 있습니다(1번 표).
- 색을 새로 쓸 일이 생기면 **두 모드 값을 같이 정해서 변수로 추가**합니다. 한쪽만 정하면 반대 모드에서 글자가 사라집니다.
- 새 화면을 만들면 반드시 두 모드에서 다 열어 봅니다.

---

## 8. 새 화면을 만들 때 검사표

하나라도 "아니오"면 아직 덜 된 것입니다.

- [ ] 색을 코드에 직접 적지 않고 변수만 썼는가
- [ ] 읽어야 하는 글자가 전부 `--text` 나 `--text-mid` 인가
- [ ] 12.5px 보다 작은 글자가 없는가 (숫자 배지 제외)
- [ ] 문제 지문이 선택지·해설보다 큰가
- [ ] 문제 카드 안의 순서가 1~7 그대로인가
- [ ] 정답/오답을 색뿐 아니라 글자로도 알려 주는가
- [ ] 틀렸을 때 정답이 무엇인지 같이 보여 주는가
- [ ] 파랑으로 채운 버튼이 화면에 하나뿐인가
- [ ] 누르는 자리가 전부 44px 이상인가
- [ ] 움직이는 것이 하나도 없는가 (색 전환 제외)
- [ ] 그림자가 카드에만 있는가
- [ ] 굵기 300 이하 글자가 없는가
- [ ] 밝은 모드와 어두운 모드 둘 다 열어 봤는가
- [ ] 폰 폭(390px)으로 줄여서 가로 스크롤이 안 생기는지 봤는가

---

## 9. 이 규칙에 맞춰 고친 내역

2026-09-12 에 사이트를 이 규칙으로 검사해 아래를 고쳤습니다.
고친 자리마다 코드에 어느 규칙 때문인지 주석을 한 줄씩 남겼습니다.

| 곳 | 고친 내용 | 어느 규칙 |
|---|---|---|
| `index.html` `.btn:hover` | 마우스를 올리면 버튼이 1px 떠오르던 것(`transform: translateY(-1px)`)을 없애고 색 전환만 남김 | 6. 애니메이션 |
| `index.html` `.btn-primary` | 파란 버튼 아래의 파란 그림자 한 겹 제거 | 6. 그림자 |
| `index.html` `header.hero` | 배경에 깔려 있던 파란 그라데이션 제거 | 6. 바탕 무늬 |
| `app/css/style.css` `.bar > i` | 진도 막대가 차오르던 `transition: width .3s` 제거 | 6. 애니메이션 |
| `app/css/style.css` `.explain .src` | 해설 끝 출처 한 줄을 `--text-dim`(3.1:1) → `--text-mid`(6:1) | 1. 대비 |
| `app/css/style.css` `.row-sub` | 목록 아래 설명을 `--text-dim` 12px → `--text-mid` 12.5px | 1. 대비 · 2. 글자 크기 |

고치지 않고 그대로 둔 것:

- `--accent` 위의 흰 글자는 대비가 4.5:1 턱걸이입니다. 색을 바꾸는 대신
  **주 버튼 글자 굵기 600 이상을 유지**하는 것으로 지킵니다. 이 굵기를 낮추면 규칙 위반이 됩니다.

### 이 규칙으로 만든 파일

- `시험대비.html` — 선박기관 시험 대비 문제 19개. 파일 하나로 혼자 열립니다.
  위 규칙을 전부 지켜 만들었고, 외부에서 받아오는 것이 하나도 없습니다.

### 이 규칙은 기계가 검사합니다

`검사.mjs` 가 **1번 표의 색과 2번 표의 글자 크기를 이 파일에서 직접 읽어**
`시험대비.html` 이 그 값만 썼는지 셉니다. 표를 고치면 검사 기준도 같이 바뀝니다.

```
node 검사.mjs
```

무엇을 완료로 볼지는 `완료기준.md` 에 있습니다.
다만 **6번(애니메이션·그림자·얇은 글씨)과 3~5번의 배치 규칙은 아직 기계가 세지 않습니다.**
그 부분은 8번 검사표를 눈으로 훑어야 합니다.
