# 만들어 온 기록

**시험기간 화이팅!** 을 어떻게 만들었는지, 무엇을 왜 그렇게 정했는지 적어 둔 문서입니다.
나중에 "이거 왜 이렇게 돼 있지?" 싶을 때 여기를 보세요.

작업일: 2026년 9월 11일 · 목포해양대학교 AI 교육 2차 워크숍

---

## 지금 어디에 있나

| 주소 | 성격 |
|---|---|
| <https://gijioo0604-hash.github.io/Exam-Period-Preparation-page/> | **본진.** 고치면 자동 반영 |
| <https://exam-period-fighting.vercel.app/> | Vercel. 수동으로 다시 올려야 함 |
| <https://github.com/gijioo0604-hash/Exam-Period-Preparation-page> | 소스 저장소 (공개) |

> 두 주소의 **학습 기록은 서로 다릅니다.** 브라우저가 주소별로 따로 저장하기 때문입니다.
> 한쪽을 정해서 쓰세요. 섞어 쓰면 기록이 갈라집니다.

---

## 어떻게 시작했나

처음 요청은 "시험 대비 사이트를 만들고 싶은데 어떻게 하면 좋을까" 였습니다.
마침 워크숍 실습자료(`실습파일/학습자료/`)의 항해학 · 선박기관 · 해사법규 요약본이
머리말에 *"시험 대비 자동 질문 웹 만들기"* 용도라고 적혀 있어서, 그걸 문제 은행의 원본으로 삼았습니다.

몇 가지를 여쭤 방향을 잡았습니다.

- **문제만 내는 사이트가 아니라** 시험기간 전체(일정 · 과제 · 대외활동 · 진도)를 관리하는 곳으로
- 문제 유형은 **5가지 전부**, 난이도는 3단계
- 문제는 샘플만 넣고 나머지는 직접 채우기

그래서 "문제 생성기"가 아니라 **시험기간 통합 대시보드**로 설계했습니다.

---

## 무엇을 어떤 순서로 만들었나

### 1. 뼈대 (첫 커밋)

화면 9개짜리 정적 웹앱. 빌드 도구 없이 `index.html` 을 더블클릭하면 열리게 만들었습니다.

**중요한 설계 결정**: 데이터를 `fetch()` 가 아니라 `<script src>` 로 불러옵니다.
`fetch` 를 쓰면 `file://` 로 열었을 때 CORS 에 막혀 동작하지 않기 때문입니다.
덕분에 인터넷 없이 파일만 있어도 앱이 돕니다.

샘플 30문제 — 5개 유형과 3단계 난이도가 모두 한 번씩은 나오도록 배치해서,
각 화면이 어떻게 도는지 바로 확인할 수 있게 했습니다.

### 2. 달력 · 기간 일정

월간 달력(일정 화면)과 주간 띠(홈)를 넣었습니다.

과제나 대외활동은 하루에 끝나지 않는다는 지적을 받아 **시작일 ~ 마감일**을 지원하게 했습니다.
D-day 도 상황에 따라 다르게 나옵니다 — `시작 D-3` / `진행 중 · 마감 D-4` / `오늘 마감` / `3일 지남`.

### 3. 공부 계획 · 진도 개편

"문제 풀이 말고 교재 읽기 같은 것도 적고 싶다" 는 요청에 따라
**과목별 할 일 목록**을 넣고, 진도 막대를 **칸 나눈 형태**로 바꿨습니다.
단원 하나가 한 무리, 무리마다 여러 칸. 칸을 눌러도 체크됩니다.

과목 · 단원을 화면에서 추가하고 지울 수 있게 했습니다.
`data/curriculum.js` 에 적힌 기본 항목은 파일을 못 고치니 **숨김 처리**하고
언제든 되돌릴 수 있게 했습니다.

### 4. 자료로 문제 만들기

"PDF 로 문제 만드는 걸 편하게" 라는 요청.

**이 사이트는 PDF 를 스스로 읽지 못합니다.** AI 호출이 필요해서 API 키와 요금이 듭니다.
대신 AI 와의 왕복을 줄였습니다 — 요청문을 만들어 주고, 돌아온 JSON 을 붙여넣으면 한 번에 등록.
AI 가 ```json 으로 감싸거나 앞뒤에 설명을 붙여도 걸러냅니다.

### 5. 구글 캘린더 (선택)

본인은 안 쓰더라도 남이 쓸 수 있게 만들어 달라는 요청.
**처음 한 번만 물어보고**, 안 쓰겠다고 하면 다시 묻지 않습니다.

설정이 필요 없는 방법 2개(일정별 링크 · `.ics` 파일)를 위에 두고,
클라이언트 ID 가 필요한 API 방식을 아래에 뒀습니다.

### 6. 로그인 · 동기화 (선택)

"폰과 노트북 기록을 맞추고 싶다".

**로그인만으로는 해결되지 않습니다.** 기록이 브라우저 안에만 있어서,
로그인해도 다른 기기에 나타나지 않습니다. 서버가 필요하고 로그인은 그 기록의 주인을 가리는 수단일 뿐입니다.
그래서 Firebase 를 붙였습니다.

합치기는 **지우는 것보다 남기는 쪽**을 택합니다. 한쪽에서 지운 것이 되살아날 수 있어
`이 기기 것으로 덮어쓰기` 를 탈출구로 뒀습니다.

> 아직 켜지 않았습니다. Firebase 프로젝트를 만들어야 쓸 수 있습니다.

### 7. 웹에 올리기

GitHub 저장소 → 공개 전환 → GitHub Pages.
PWA(홈 화면에 추가 · 오프라인)와 소개 페이지, QR 공유를 붙였습니다.

좁은 화면에서 사이드바가 화면을 다 잡아먹어서, **가로 스크롤 알약 메뉴**로 바꿨습니다.

---

## 고친 버그들 (기억해 둘 만한 것)

검토를 여러 번 돌리면서 나온 것 중, 나중에 또 만날 법한 것들입니다.

### 배포해도 고친 코드가 반영되지 않았다

가장 오래 헤맨 문제입니다. 서버에는 새 코드가 있는데 브라우저가 옛 코드를 실행했습니다.

원인이 **두 겹**이었습니다.

1. `<script src="js/store.js">` 에 버전이 없어 브라우저 HTTP 캐시가 재사용
2. GitHub Pages 가 **HTML 자체**에 10분 캐시를 걸어, 예전 HTML 이 예전 버전을 가리킴

→ 모든 참조에 `?v=12` 를 붙이고, 서비스 워커가 HTML 은 캐시를 건너뛰고 받게 했습니다.

**그래서 파일을 고쳐 올릴 때는 `app/index.html` 의 `?v=` 와 `sw.js` 의 `CACHE` 버전을 같이 올려야 합니다.**

### 화면이 멈췄다

문제를 다 푼 뒤 `[같은 조건으로 새 세션]` 을 눌러도 아무 일도 없었습니다.
브라우저는 **지금 주소와 같은 링크**를 누르면 아무것도 하지 않습니다.
`#/quiz` 에서 시작한 세션이라 돌아갈 주소도 `#/quiz` 였던 것.

→ 주소가 같으면 직접 다시 그리도록 고쳤습니다.

### 저장 실패가 조용히 넘어갔다

`localStorage` 쓰기가 실패하면 콘솔에만 찍혔습니다. 화면에는 저장된 것처럼 보이고요.
**쌓인 기록이 전부인 앱에서 이건 조용한 데이터 유실**입니다.

→ 공간 부족 / 시크릿 모드를 구분해 알려 줍니다.

### 같은 클릭이 여러 번 처리됐다

화면을 다시 그릴 때마다 이벤트 핸들러를 새로 달았습니다.
4지선다에서 **이전 문제의 정답으로 채점**되는 일이 실제로 일어났습니다.

→ (이벤트 + 선택자) 하나당 핸들러 하나만 남기도록 바꿨습니다.

### 태블릿에서 날짜 칸이 겹쳤다

라벨이 세로 flex 라 `(선택)` 힌트가 딴 줄로 떨어지고,
힌트가 붙은 칸만 입력이 아래로 밀려 옆 칸과 어긋났습니다.

→ 라벨을 블록으로 바꾸고, 900px 이하에서는 한 줄에 하나씩 놓습니다.

### `javascript:` 링크가 걸렸다

단원 자료 경로를 검증 없이 `<a href>` 에 넣었습니다.
내가 친 값만 있으면 자기 브라우저 얘기지만, **백업 가져오기는 남의 JSON 을 받습니다.**

→ `http` / `https` / `file` 과 상대 경로만 통과. 저장된 값도 화면에 걸기 직전에 한 번 더 거릅니다.

### 과목 이름에 `::` 를 넣으면 데이터가 엉켰다

내부적으로 `"과목::단원"` 으로 키를 만드는데, 이름에 `::` 가 있으면 되돌릴 때 잘못 쪼개졌습니다.

→ 이름에 `::` 를 못 쓰게 막았습니다.

### Vercel 설정이 앱을 통째로 깨뜨릴 뻔했다

`vercel.json` 에 `trailingSlash: false` 를 넣었는데, 이러면 `/app/` 이 `/app` 으로 바뀝니다.
앱은 `css/style.css` 처럼 상대 경로로 부르므로 기준이 `/app` 이 되면 `/css/style.css` 를 찾습니다 — 없는 주소.
**스타일도 스크립트도 안 붙어 빈 화면이 됐을 것입니다.**

→ `trailingSlash: true` 로 고쳤습니다. 실제 배포에서 `/app` → `/app/` 로 넘어가는 것 확인.

---

## 하다가 막힌 것 · 하지 않기로 한 것

### 워크숍 자료는 저장소에서 뺐습니다

처음엔 전부 올렸다가, **"내가 만든 게 아닌 건 빼 달라"** 는 요청에 따라
교안 PDF · 실습파일 · 요청문 모음을 제외했습니다.
커밋이 1개뿐이던 시점이라 **이력까지 새로 써서** 공개 저장소에 흔적이 남지 않게 했습니다.
파일은 내 컴퓨터에 그대로 있고 `.gitignore` 로 앞으로도 안 올라갑니다.

### Vercel 을 GitHub 연결 없이 올리기

네 가지를 시도해서 앞의 세 개가 막혔습니다.

| 방법 | 결과 |
|---|---|
| Git 저장소 가져오기 | 깃허브 · 깃랩 · 비트버킷 전부 앱 연결 요구 |
| 저장소 주소 입력 | 새 비공개 저장소를 복사 생성하는 방식 |
| 로컬 서버에서 파일 가져오기 | 브라우저가 외부 사이트 → 내 컴퓨터 접근을 차단 |
| **파일을 배포 칸에 직접 넣기** | **성공** |

마지막 방법도 처음엔 `index.html이 두 번 나타납니다` 로 거부당했습니다.
폴더 구조가 사라져 루트의 `index.html` 과 `app/index.html` 이 충돌한 것.
→ 파일마다 원래 폴더 경로를 다시 심는 장치를 걸고 다시 넣어 통과했습니다.

### 제가 대신 못 하는 것

계정 생성과 비밀번호를 다루는 일은 하지 않습니다. 그래서 이 셋은 직접 하셔야 합니다.

- Firebase 프로젝트 만들기 (동기화용)
- 구글 OAuth 클라이언트 ID 발급 (캘린더 API 방식용)
- Vercel · GitHub 계정 로그인

---

## 아직 안 한 것

셋 다 **안 해도 앱은 완전히 정상**입니다.

| 항목 | 하면 좋아지는 것 | 필요한 준비 |
|---|---|---|
| **로그인 · 동기화** | 폰 ↔ 노트북 기록 자동 일치 | Firebase 프로젝트 |
| **구글 캘린더 API** | 버튼 한 번으로 캘린더에 등록 | OAuth 클라이언트 ID |
| **실제 시험 문제** | 샘플 30개 → 내 시험 범위 | 교재 PDF 를 AI 에 넘기기 |

지금은 **백업 · 초기화** 화면의 파일 내보내기/가져오기로 기기 간 이동이 됩니다.

---

## 앞으로 고칠 때

```bash
git add -A
git commit -m "무엇을 바꿨는지"
git push
```

푸시하면 GitHub Pages 는 1~2분 안에 반영됩니다.

**버전 두 곳을 같이 올리세요.** 안 올리면 브라우저가 예전 파일을 계속 씁니다.

1. `app/index.html` 의 `?v=12` → `?v=13` (전체 바꾸기)
2. `sw.js` 의 `var CACHE = "examhub-v12"` → `"examhub-v13"`

**Vercel 주소는 자동이 아닙니다.** 고친 뒤 *"Vercel에도 올려줘"* 라고 말씀하시면 다시 올립니다.

---

## 커밋 이력

| | |
|---|---|
| `da33766` | 웹앱 + 소개 페이지 (첫 커밋) |
| `c656fa7` | 과목 · 단원 지우기, 검토 수정 |
| `c9d6311` | 입력 검증 — 링크 스킴 제한, 이름 구분자 금지 |
| `0a6723b` | 배포해도 고친 코드가 반영되지 않던 문제 |
| `10e3576` | 같은 주소 링크로 화면이 멈추던 문제, 저장 실패 무음 처리 |
| `1bf7dde` | 태블릿에서 날짜 · 마감일 칸이 겹치던 문제 |
| `606619e` | 새로 올린 화면이 10분 늦게 나오던 문제 |
| `749b879` | 구글 캘린더 연동, 마감일 힌트 제거 |
| `728323a` | 구글 캘린더 패널을 아래로 내리고 접어 둠 |
| `2541632` | 이름을 '시험기간 화이팅!' 으로, [연결 안 함] 추가 |
| `4c232fa` | 로그인 · 기기 간 동기화 (Firebase) |
| `4e2001e` | 사이드바 [설정] 묶음, 동기화 코드 검토 수정 |
| `8e5a211` | Vercel 배포 준비 |
| `c566531` | Vercel 에서 앱이 깨질 설정 수정 |

---

## 한 줄 요약

브라우저만 있으면 되는 시험기간 관리 웹앱. 화면 11개, 문제 유형 5가지, 샘플 30문제.
기록은 브라우저 안에만 저장되고 서버로 나가지 않습니다.
로그인 · 캘린더 연동은 **선택**이고, 안 해도 전부 동작합니다.
