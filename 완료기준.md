# 완료기준.md — 시험대비.html

무엇이 되어 있어야 **다 만든 것으로 볼지**를 정한 문서입니다.

- 네 문장 전부 "예"가 되어야 끝난 것으로 봅니다.
- 판정은 눈으로 하지 않고 `검사.mjs` 가 셉니다. 사람마다 다르게 볼 여지를 남기지 않기 위해서입니다.
- 기준을 바꾸고 싶으면 **이 파일을 먼저 고칩니다.** `검사.mjs` 는 이 파일의 `기준N` 을 읽어
  자기가 검사하는 항목과 대조하고, 여기 적혀 있는데 검사하지 않는 기준이 있으면 실패로 처리합니다.

관련 문서: 모양 규칙은 [디자인.md](디자인.md), 검사기는 [검사.mjs](검사.mjs) 입니다.

---

## 완료 기준

- [x] 기준1 · 문제가 10개 이상이다
- [x] 기준2 · 모든 문제에 정답과 해설이 있다
- [x] 기준3 · 모든 해설에 출처 파일명(`.md`)이 적혀 있다
- [x] 기준4 · 디자인.md 표에 있는 글자 크기와 색만 쓴다

> 2026-09-12 `검사.mjs` 기준 네 항목 모두 통과. 체크 표시는 그날의 결과이고,
> 진짜 판정은 지금 다시 돌려서 나오는 결과입니다.

---

## 확인 방법

각 기준을 **무엇을 세어서** 판정하는지 적습니다. 세는 방법이 정해져야 기준입니다.

| 기준 | 무엇을 보는가 | 어떻게 세는가 | 통과 조건 |
|---|---|---|---|
| 기준1 | 문제 개수 | `시험대비.html` 안 `QUESTIONS` 배열의 길이 | 10 이상 |
| 기준2 | 정답 | 각 문제의 `answer` 가 보기 개수 범위 안의 정수인가 | 전부 그렇다 |
| 기준2 | 해설 | 각 문제의 `explain` 이 빈 문자열이 아닌가 | 전부 그렇다 |
| 기준3 | 출처 | 각 문제의 `src` 에 `.md` 로 끝나는 파일명이 있는가 | 전부 그렇다 |
| 기준4 | 색 | 파일에 쓴 hex 색이 전부 디자인.md 1번 표에 있는가 | 표 밖의 색 0개 |
| 기준4 | 글자 크기 | 파일에 쓴 `font-size` 가 전부 디자인.md 2번 표에 있는가 | 표 밖의 크기 0개 |

기준2와 기준4는 각각 두 줄로 나누어 셉니다. 정답이 빠진 것과 해설이 빠진 것은
고치는 방법이 다르고, 색이 틀린 것과 크기가 틀린 것도 마찬가지이기 때문입니다.

### 돌리는 법

```
node 검사.mjs
```

실패가 하나라도 있으면 종료 코드 1 로 끝납니다.
실패한 항목에는 **몇 번 문제인지** 번호가 함께 나옵니다.

---

## 이 문서가 판정하지 않는 것

기계가 셀 수 없는 것은 기준에 넣지 않았습니다. 넣으면 "통과"라는 말이 헐거워집니다.
아래는 **사람이 직접 봐야 하는 것**이고, 검사기를 통과했다고 해결되지 않습니다.

- 해설 내용이 실제로 자료와 맞는가 (근거 문장을 하나씩 대조해야 합니다)
- 문제가 시험에 나올 만한가, 난이도 표시가 적당한가
- 자료에 없는 내용을 지어내지 않았는가 — 출처가 **적혀 있는지**는 세지만,
  그 출처에 정말 그 내용이 있는지는 세지 못합니다
- 어두운 모드에서 실제로 읽히는가 (두 모드로 한 번씩 열어 봐야 합니다)

---

## 범위 밖 (이번에는 하지 않는 것)

- 점수·오답 기록을 저장하는 일 (파일 하나로 열리는 것이 우선입니다)
- 문제를 자동으로 더 만들어 내는 일
- 인터넷에서 무언가를 받아오는 기능 — **금지**이지 미룬 것이 아닙니다
