# 작업일지 — 시험기간 화이팅!

AI 와 대화하며 웹앱을 만든 과정을 요청 순서대로 정리한 기록입니다.

- **일시** 2026년 9월 11일
- **과정** 목포해양대학교 AI 교육 · 2차 AI 바이브코딩
- **결과물** <https://gijioo0604-hash.github.io/Exam-Period-Preparation-page/>
- **소스** <https://github.com/gijioo0604-hash/Exam-Period-Preparation-page>

> 설계 의도와 버그의 원인을 더 자세히 본 문서는 [HISTORY.md](HISTORY.md) 에 있습니다.
> 이 문서는 **무엇을 요청했고 무엇을 했는지**를 시간 순으로 적은 것입니다.

---

## 1. 시작 — 무엇을 만들지 정하기

**요청** 대학생인데 시험대비 사이트를 만들고 싶다. 어떻게 하면 좋을까?

**대응** 워크숍 실습자료(`실습파일/학습자료/`)의 항해학 · 선박기관 · 해사법규 요약본이
머리말에 *"시험 대비 자동 질문 웹 만들기"* 용도라고 적혀 있는 것을 발견하고 이를 원본으로 제안.
학습 방식 · 문제 채우는 방법 · 과목 범위를 여쭤 방향을 정함.

**정해진 것**
- 문제 유형 5가지 전부 (4지선다 · OX · 플래시카드 · 단답 · 서술형), 난이도 3단계
- 샘플 문제만 넣고 나머지는 직접 추가
- 3과목 모두 다루고, 이후 PDF 기반으로 확장

---

## 2. 범위 확장 — 문제 생성기가 아니라 통합 대시보드로

**요청** 문제 만들어주는 사이트에 국한하지 말고, 시험기간의 모든 것을 대비하고 싶다.
일정, 오답 기반 약점 보완, 공부 계획과 진도 체크까지.

**대응** 설계를 바꿔 화면 9개짜리 앱으로 만듦.

| 화면 | 내용 |
|---|---|
| 홈 | D-day, 오늘 할 일, 학습 현황, 약한 단원 |
| 일정 · D-day | 시험 · 과제 · 대외활동 통합 관리 |
| 공부 계획 · 진도 | 단원 체크리스트, 단원별 정답률 |
| 문제 풀기 | 과목 · 단원 · 유형 · 난이도 조합 출제 |
| 약점 보완 퀴즈 | 오답률 높은 단원 집중 출제 |
| 오답노트 | 자동 수집, 메모, 연속 2회 정답 시 극복 |
| 학습 통계 | 일별 · 과목별 · 유형별 정답률 |
| 문제 추가 · 편집 | 직접 문제 만들기 |
| 백업 · 초기화 | 기록 내보내기 / 가져오기 |

**미리 알린 제약** 서술형은 자동 채점이 불가능해서, 모범답안과 채점 키워드를 펼쳐 놓고
본인이 맞음/틀림을 고르는 방식으로 만듦.

---

## 3. 달력과 일정 개선

**요청** 월별 달력을 일정 추가 위에. 홈에는 일주일 달력을 작게.
과목별 진도율도 따로. 그리고 질문 몇 가지.

**질문에 답한 것**
- *"이름이 같아야 한다는 게 무슨 소리냐"* → 화면에서 고르면 신경 쓸 필요 없음.
  파일을 손으로 고칠 때만 해당. 겁주는 문구라 판단해 삭제하고, 실제로 어긋났을 때만 경고가 뜨도록 변경
- *"왜 진도 화면에 문제 풀기가 있냐"* → 체크 전 확인용. 다만 지적이 맞아 작게 내림

**추가한 것** 월간 달력(빈 칸 클릭 → 날짜 자동 입력, 일정 클릭 → 수정),
홈 주간 띠 달력, 과목별 공부 계획, 과목 · 단원 추가, 문제 풀기 세션 기록

---

## 4. 기간 일정과 진도 표시

**요청** 과제나 대외활동은 하루에 안 끝나니 시작일과 마감일을 따로.
과목별 진도율은 모아두지 말고 각 과목 위에. 주간 달력 날짜는 작게.

**대응** 시작일 ~ 마감일 지원. 달력에서 띠처럼 이어지게 표시.
D-day 도 상태별로 다르게 (`시작 D-3` / `진행 중 · 마감 D-4` / `오늘 마감` / `3일 지남`).

---

## 5. 진도 막대를 칸 나눈 형태로

**요청** 숫자만 말고 막대표로. → 칸이 크니 더 잘게. → 칸 수를 더 늘려줘.

**대응** 세 번에 걸쳐 다듬음. 최종적으로 **단원 하나가 한 무리, 무리마다 여러 칸**,
전체가 24칸쯤 되도록 자동 조절. 칸을 눌러도 체크되게 함.

---

## 6. 자료로 문제 만들기

**요청** 그 안내 문구가 무슨 소리인지 모르겠다. PDF 를 문제로 바꾸는 걸 편하게 해달라.

**대응** 문제가 된 문구는 제가 잘못 쓴 것이었음 — *"저에게 주면서"* 의 '저'가 누구인지
웹사이트만 보면 알 수 없는 문장이었음. 삭제.

**새 화면 추가** — 이 사이트는 PDF 를 스스로 읽지 못한다는 점을 먼저 알리고,
AI 와의 왕복을 3단계로 줄임.

1. 요청문 복사 (현재 과목 · 단원 목록과 형식이 들어감)
2. AI 에 PDF 와 함께 붙여넣기
3. 돌아온 결과를 붙여넣고 미리보기 → 추가

AI 가 코드펜스로 감싸거나 앞뒤에 설명을 붙여도 걸러내도록 함.

---

## 7. 깃허브에 올리기

**요청** `Exam-Period-Preparation-Page` 저장소에 저장하고 연동해달라.

**대응** 저장소가 이미 비어 있는 상태로 만들어져 있어서 그대로 사용.
`gh` CLI 는 없었지만 저장된 자격 증명으로 푸시 성공.

**처음엔** 워크숍 폴더 전체를 올렸으나, 이후 *"내가 만든 게 아닌 건 빼달라"* 는 요청에 따라
교안 PDF · 실습파일 · 요청문 모음을 제외. 커밋이 1개뿐이던 시점이라
**이력까지 새로 써서** 공개 저장소에 흔적이 남지 않게 함.

---

## 8. 웹에 올리고 폰에서 쓰기

**요청** 웹에 올려서 폰·태블릿으로도 쓰고 싶다. 홈페이지처럼 만들고 공유하기 편하게.

**대응**
- GitHub Pages 로 공개 배포
- **소개 페이지** 신설 (기능 소개, 설치 안내, QR 코드, 공유 버튼)
- **PWA** — 홈 화면에 추가하면 전체화면으로 열리고 오프라인에서도 동작
- 앱 아이콘 생성
- 좁은 화면에서 사이드바가 화면을 다 잡아먹어 **가로 스크롤 알약 메뉴**로 변경
- 공유 편하도록 폴더 이름을 `app` 으로 변경 (한글 폴더명은 주소에서 깨짐)

---

## 9. 검토 (여러 차례)

요청하실 때마다 검토를 돌렸고, **실제로 동작을 깨뜨리는 버그를 여러 개** 찾아 고쳤습니다.

| 버그 | 증상 |
|---|---|
| 이벤트 핸들러 중복 | 4지선다에서 **이전 문제의 정답으로 채점**됨 |
| 같은 주소 링크 | 문제를 다 푼 뒤 버튼을 눌러도 **결과 화면에 갇힘** |
| 저장 실패 무음 | 저장 안 됐는데 **된 것처럼 보임** (조용한 데이터 유실) |
| 캐시 반영 안 됨 | 고친 코드를 올려도 **예전 코드가 실행됨** |
| 과목 전체 삭제 | 문제 편집 화면이 **빈 화면**이 됨 |
| `javascript:` 링크 | 남의 백업 파일을 가져올 때 **코드가 실행될 수 있었음** |
| 이름에 `::` | 진도 데이터가 **엉뚱하게 쪼개짐** |
| 태블릿 레이아웃 | 날짜 칸과 마감일 칸이 **겹쳐 보임** |

특히 **캐시 문제**는 원인이 두 겹이었습니다. 파일 주소에 버전이 없어 브라우저가 재사용한 것과,
GitHub Pages 가 HTML 자체에 10분 캐시를 거는 것. 둘 다 고쳤습니다.

---

## 10. 구글 캘린더 연동 (선택)

**요청** 마감일 옆 힌트 제거. 그리고 구글 캘린더와 연동. 단, 연동 안 해도 쓸 수 있게 하고
연동할지는 한 번만 물어보도록.

**대응** 처음 한 번만 묻고, 안 쓴다고 하면 다시 묻지 않음.
설정이 필요 없는 방법 2개를 위에, API 방식을 아래에 배치.

| 방법 | 설정 |
|---|---|
| 일정마다 캘린더 버튼 | 없음 |
| `.ics` 파일로 한 번에 | 없음 |
| API 로 바로 넣기 | 클라이언트 ID 필요 |

이후 *"연동 안 할 거면 '연결 안 함' 선택지도 있어야 한다"* 는 지적에 따라
버튼 문구를 명확히 하고, 연결한 뒤에도 끊을 수 있게 함.

---

## 11. 이름 변경

**요청** '시험기간 대비 허브' 를 '시험기간 화이팅!' 으로.

**대응** 탭 제목, 사이드바, 소개 페이지, 홈 화면 아이콘 이름, 백업 파일명,
캘린더 파일의 만든이 항목까지 전부 변경.

---

## 12. 로그인 · 기기 간 동기화 (선택)

**요청** 로그인 기능을 만들고 싶다.

**먼저 알린 것** — **로그인만으로는 해결되지 않습니다.**
기록이 브라우저 안에만 있어서 로그인해도 다른 기기에 나타나지 않습니다.
서버가 필요하고, 로그인은 그 기록의 주인을 가리는 수단일 뿐입니다.

목적을 여쭈니 **폰 ↔ 노트북 동기화**여서 Firebase 를 붙였습니다.

합치기는 **지우는 것보다 남기는 쪽**을 택하도록 설계.
한쪽에서 지운 것이 되살아날 수 있어 `이 기기 것으로 덮어쓰기` 를 탈출구로 둠.

**요청** 너가 다 만들어주면 안 되나?
**답** 구글 계정 로그인은 비밀번호를 다루는 일이라 제가 할 수 없음.
크롬을 대신 조작해 드릴 수는 있다고 제안.
→ *"내 걸로는 지금 로그인 안 할래"* 하셔서 기능만 만들어 두고 켜지 않음.

---

## 13. 사이드바 정리

**요청** '계정 동기화' 와 '백업 초기화' 를 '설정' 이라는 큰 주제로 묶어줘.

**대응** `[문제] / [관리] / [설정]` 세 묶음으로 정리.
폰의 가로 메뉴에서는 묶음 이름이 숨겨져 있어 **가는 세로선**으로 구분되게 함.

---

## 14. Vercel 배포

**요청** Vercel 이 뭐야? 써보고 싶은데 해줘.

**설명한 것** GitHub Pages 와 같은 일을 하되 서버 코드 실행과 캐시 제어가 가능.
다만 **지금 사이트에 꼭 필요하지는 않다**고 솔직히 말씀드림.

**준비** `vercel.json` 작성. 이 과정에서 **앱을 통째로 깨뜨릴 설정**을 스스로 발견해 수정
(`trailingSlash` 때문에 `/app/` 이 `/app` 으로 바뀌면 상대 경로가 깨져 빈 화면이 됨).

**막힌 것** 깃허브 연결 없이 올리려고 네 가지를 시도해 셋이 막힘.

| 시도 | 결과 |
|---|---|
| Git 저장소 가져오기 | 모든 공급자가 앱 연결 요구 |
| 저장소 주소 입력 | 새 비공개 저장소를 복사 생성하는 방식 |
| 로컬 서버에서 가져가기 | 브라우저가 외부 사이트 → 내 컴퓨터 접근 차단 |
| **파일을 배포 칸에 직접 넣기** | **성공** |

마지막도 처음엔 `index.html이 두 번 나타납니다` 로 거부됨.
폴더 구조가 사라져 루트와 `app/` 의 `index.html` 이 충돌한 것.
→ 파일마다 원래 폴더 경로를 다시 심는 장치를 걸어 통과.

**결과** <https://exam-period-fighting.vercel.app>

---

## 15. 기록 남기기

**요청** 지금까지 대화를 바탕으로 문서를 만들어줘.

**대응** [HISTORY.md](HISTORY.md) (설계 의도와 버그 원인 중심)와
이 문서 [작업일지.md](작업일지.md) (요청 순서 중심)를 작성해 저장소에 올림.

---

## 최종 결과

### 만들어진 것

- **화면 11개**, 문제 유형 5가지, 난이도 3단계, 샘플 30문제
- 빌드 도구 없음. 파일을 고치고 새로고침하면 끝
- 기록은 브라우저 안에만 저장되고 서버로 나가지 않음
- 폰 홈 화면에 추가하면 앱처럼 열리고 오프라인에서도 동작

### 커밋 15개 · 파일 34개

### 남겨둔 것 (셋 다 안 해도 정상 동작)

| 항목 | 필요한 준비 |
|---|---|
| 로그인 · 동기화 | Firebase 프로젝트 |
| 구글 캘린더 API | OAuth 클라이언트 ID |
| 실제 시험 문제 | 교재 PDF 를 AI 에 넘기기 |

---

## 이 작업에서 배운 점

**AI 에게 맡길 수 있는 것과 없는 것이 나뉩니다.**
코드 작성 · 버그 찾기 · 배포 설정은 맡길 수 있지만,
계정 생성과 비밀번호를 다루는 일은 직접 해야 합니다.

**"만들어졌다" 와 "제대로 된다" 는 다릅니다.**
검토할 때마다 동작을 깨뜨리는 버그가 나왔고, 그중 몇 개는
실제로 써 보기 전에는 드러나지 않았을 것들이었습니다.
특히 자동 검사에서 "정상" 으로 나왔지만 실제로는 오류 화면이 뜨던 경우가 있어,
검사 방법 자체를 고친 일도 있었습니다.

**제약을 먼저 말하는 편이 낫습니다.**
서술형 자동 채점 불가, PDF 직접 읽기 불가, 로그인만으로는 동기화 안 됨 —
이런 것들을 먼저 알리고 대안을 만든 덕분에 헛수고를 줄였습니다.

---

## 16. 지어낸 문제를 막고, 인터넷을 끊는다

### 요청

> 문제 만들 때 무조건 10문제 이상으로 만들어주게 수정해주고
> 각 문제에 정답과 해설도 만들어줘야해.
> 그리고 해설 끝에 어떤 파일의 어느 점에서 나왔는지 표시 해줘야 하고,
> 자료에 없는 내용으로 문제 절대 만들면 안되고,
> 자료에 내용이 없으면 "자료에 없음"이라고 적어야 하도록 해야하고
> 자료 없는데 있다고 허위 정보를 주지 않도록 수정해줘.
> 그리고 인터넷에서 무언가를 불러오도록 하면 안되고
> 무조건 파일 안에 있는 내용으로만, 혼자서 열려야 하도록 만들어줘.

### 무엇이 문제였나

만들어 둔 것을 뜯어보니 세 가지가 걸렸습니다.

| 문제 | 상태 |
|---|---|
| 문제에 출처 항목이 아예 없음 | 30문제 전부 |
| 해설이 빈 문제 | 5개 (전부 플래시카드) |
| 인터넷에서 받아오는 것 | 첫 화면의 QR 라이브러리 (cdnjs) |

QR 라이브러리가 제일 곤란했습니다. **켤 때마다 무조건** 받아왔기 때문에,
첫 화면에 적어 둔 "서버로 나가지 않습니다" 가 사실이 아니었습니다.

### 출처

문제마다 `source` 항목을 넣었습니다. 서식은 **파일이름 · 항목**.

```js
{
  id: "nav-002",
  subject: "항해학", unit: "방위와 침로",
  source: "항해학_기초.md · 2. 방위와 침로",
  ...
}
```

해설을 그리는 곳이 문제 풀기 · 오답노트 두 군데로 나뉘어 있어서,
한쪽만 고치면 화면마다 다르게 나옵니다. `App.explainHtml()` 하나로 합치고
두 화면이 모두 그걸 쓰게 했습니다.

### "자료에 없음"

없는 것을 있다고 하지 않게 하는 게 핵심이었습니다.
그래서 **빈 칸을 허용하지 않고, 없으면 없다고 쓰게** 했습니다.

- 해설이 없으면 → 일괄 등록에서 그 문제를 건너뜀, 편집 화면에서는 저장이 안 됨
- 출처가 없거나 `"자료에 없음"` 이면 → 노란색으로 **출처: 자료에 없음** 표시

빈 칸을 그냥 두면 "아직 안 썼다" 와 "자료에 없다" 가 구분되지 않습니다.
구분이 되어야 푸는 사람이 그 문제를 믿을지 말지 정할 수 있습니다.

### AI 에게 보내는 요청문

요청문 맨 앞에 **지어내지 말라**는 규칙을 올렸습니다.

- 첨부한 자료에 적힌 것만 쓸 것, 따로 아는 지식을 보태지 말 것
- 근거를 못 찾으면 "자료에 없음" 이라고 적을 것
- 개수를 못 채우겠으면 **억지로 채우지 말고** 만들 수 있는 만큼만 줄 것
- 모든 문제에 정답 · 해설 · 출처를 넣을 것
- 최소 10문제

마지막 항목이 앞 항목들과 부딪힙니다. "10개 채워라" 와 "없으면 만들지 마라" 중
**만들지 마라가 우선**이라고 요청문에도, 앱의 경고 문구에도 적었습니다.
10개가 안 되면 경고만 하고 추가는 그대로 됩니다.

### QR 을 직접 그리기

라이브러리를 받아오지 않으려면 둘 중 하나였습니다.
QR 기능을 없애거나, 직접 만들거나. 만드는 쪽을 골랐습니다 (`qr.js`).

바이트 모드 · 오류정정 M · 버전 1~9 (최대 180바이트). 주소 하나에는 넉넉합니다.

**맞게 만들어졌는지 확인하는 게 더 어려웠습니다.** 브라우저에 QR 판독기가 없어서
스캐너로 대조할 수 없었고, 내가 만든 것을 내가 만든 코드로 읽으면
같은 오해를 두 번 해도 통과합니다. 그래서 규격에 공개된 값과 대조했습니다.

| 검사 | 결과 |
|---|---|
| 형식 정보 15비트 (마스크 0~7) | 규격 표와 일치 |
| 버전 정보 18비트 (v7~v9) | 규격 표와 일치 |
| 오류정정 생성 다항식 (EC 10 · 16) | 규격 표와 일치 |
| 오류정정 신드롬 (v1~v9 전 블록) | 모두 0 |
| 되읽기 (5~180바이트) | 원문 그대로 복원 |

**여기서 진짜 버그가 하나 잡혔습니다.** 생성 다항식의 계수 순서가 뒤집혀 있어서
오류정정 코드가 통째로 틀리게 나오고 있었습니다.
되읽기 검사는 오류정정을 건드리지 않으니 **멀쩡히 통과**했고,
규격 표 대조에서만 드러났습니다. 그대로 뒀으면 화면에는 그럴듯한 QR 이 뜨고
스캐너로는 안 읽히는, 알아채기 어려운 고장이 됐을 겁니다.

### 인터넷 연결 남은 곳

전부 훑어보니 두 곳이 남았는데, 둘 다 **버튼을 눌러야 연결**합니다.

| 기능 | 켜는 시점 |
|---|---|
| 구글 캘린더 바로 넣기 | [연결하기] 를 누른 그 순간 |
| 로그인 · 동기화 | Firebase 설정을 저장하고 로그인할 때 |

지우지 않고 남겼습니다. 안 켜면 아무것도 불러오지 않고,
켜는 것은 쓰는 사람이 정하는 일이기 때문입니다.

확인은 브라우저 네트워크 기록으로 했습니다.
첫 화면 · 앱 11개 화면 · 문제 풀기 · 일괄 등록 · 편집을 다 돌아본 뒤
**바깥으로 나간 요청 0건**이었습니다.

### 이번에 배운 것

**내가 만든 것을 내가 만든 것으로 검사하면 안 됩니다.**
QR 되읽기는 통과했지만 오류정정은 틀려 있었습니다.
바깥에 공개된 기준값과 맞춰 보기 전까지는 몰랐습니다.

**빈 칸은 정보가 아닙니다.**
"해설 없음" 을 빈 문자열로 두면 안 쓴 건지 없는 건지 알 수 없습니다.
없으면 없다고 쓰게 만드는 편이, 모르는 것을 모른다고 아는 데 낫습니다.
